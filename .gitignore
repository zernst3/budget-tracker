# Rust build artifacts
/target
**/*.rs.bk

# Terraform
**/.terraform/*
*.tfstate
*.tfstate.*
*.tfvars
crash.log

# Env / secrets
.env
.env.*

# OS
.DS_Store

# Local-only cargo dev overrides (chorale Select worktree) — never commit
.cargo/
