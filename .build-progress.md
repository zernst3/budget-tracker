# Build progress worklog

Seeded from `PHASE_1_REPORT.md` §8 (Phase-2 build plan). Backend phase = steps
1–9; step 10 (chorale UI) is the separate frontend phase and is **out of scope
for this build**. Each step cites the rules it must satisfy
(`PROC-CITE-CONVENTION-ID-1` / `CC-2`).

Legend: `[x]` done · `[~]` in progress · `[ ]` not started.

---

## Backend phase

- [x] **1. Workspace + infra skeleton.**
  Cargo workspace + the five backend crates (`budget-domain`,
  `budget-entities`, `budget-mappers`, `budget-infrastructure`,
  `budget-app-services`); workspace `[lints]` (`unsafe_code = "forbid"`, clippy
  `unwrap_used`/`expect_used`/`panic` denied); Terraform (`infra/`: Azure +
  Neon + GitHub providers — resource group, Log Analytics free tier, Container
  Apps env + scale-to-zero app, Key Vault + managed-identity access, Neon
  project/branch/role/database/endpoint, Neon URL -> Key Vault secret ->
  Container App secret reference, remote-state backend stub); CI quality gate
  (`.github/workflows/ci.yml`: fmt, clippy pedantic + unwrap/expect denied,
  test, build image, push GHCR, deploy ACA on main). `cargo check --workspace`
  green; `terraform fmt` + `terraform validate` clean.
  Rules: `DOMAIN-1`, `ENTITIES-13`, `MAPPER-1`, `BUDGET-MONEY-1` (lint spine),
  `BUDGET-PLAID-TOKEN-VAULT-1` (Key Vault wiring).

- [x] **2. Schema + entities + domain types (FIELD-LEVEL SCHEMA + CONSTRAINT/MIGRATION LAYER DONE).**
  All tables from `SPEC.md` §5 (users, budgets, categories, accounts,
  plaid_items, months, transactions, funds, repayment_obligations,
  paycheck_config) including every income mode, cadence, fund kind. SeaORM
  entities (`ENTITIES-*`), domain newtypes, the `Money` type (`BUDGET-MONEY-1`),
  error enums (thiserror, `DOMAIN-*`), repo traits, mappers (`MAPPER-1`,
  `BUDGET-PLAID-SIGN-1` flip at the boundary). The field-level column schema is
  complete AND the SeaORM migration layer that materializes the §12 DB
  constraints is now built (run #2): a new `budget-migration` crate
  (`sea-orm-migration`) with a `Migrator` runner, wired into
  `budget-infrastructure::run_pending_migrations` for startup application. The
  four §12 DB constraints (partial unique on
  `categories(budget_id) WHERE is_rollover_bucket`, partial unique on
  `transactions(month_id) WHERE is_rollover`, UNIQUE on
  `transactions.plaid_transaction_id` (partial WHERE NOT NULL),
  UNIQUE `(user_id, year, month)` on `months`) are no longer prose-only —
  they exist as real DB indexes in the genesis migration.
  Rules: `DOMAIN-*`, `ENTITIES-*`, `MAPPER-1`, `BUDGET-MONEY-1`,
  `BUDGET-PLAID-SIGN-1`. **Done.**
  - `[x]` SeaORM entities (10 tables, `ENTITIES-*`) — committed `20a76ce`.
  - `[x]` Domain layer in `budget-domain` (`DOMAIN-*`, `REPO-1/2/3/10`):
    the `Money` newtype (`BUDGET-MONEY-1`, Decimal-backed, zero-drift property
    tests); newtype IDs for all 11 aggregates (`DOMAIN-2`); validated newtypes
    `Email` + `AccessTokenRef` (`DOMAIN-3`); typed domain enums mirroring the pg
    enums (`ENTITIES-12`); `ValidationError`/`RepositoryError`/`DomainError`
    (`DOMAIN-4/6`); a domain struct per aggregate (`DOMAIN-7`,
    `DateTime<Utc>`/`NaiveDate`/`Money`); one repo trait per aggregate
    (`REPO-1/2/3`) with the rollover/settlement/fund/Plaid-cursor read surfaces;
    `UnitOfWork` + `UowProvider` traits (`REPO-4/10`, object-safe boxed-closure
    shape); and the two named budget predicates as tested pure functions —
    `counts_in_budget` (`BUDGET-STATUS-DRIVES-INCLUSION-1`) +
    `fixed_category_spent` (`BUDGET-NO-DOUBLE-CHARGE-1`). 20 unit tests green.
  - `[x]` Mappers (`MAPPER-1`, `BUDGET-PLAID-SIGN-1`) — all 10 modules in
    `budget-mappers/src/` (`users`, `budgets`, `categories`, `accounts`,
    `plaid_items`, `months`, `transactions`, `funds`, `repayment_obligations`,
    `paycheck_config`). Read-path `model_to_domain` + write-path
    `domain_to_active_model` per aggregate; `Email`/`AccessTokenRef` fallible on
    read; `transactions::plaid_model_to_domain` flips the Plaid sign once at the
    boundary (`BUDGET-PLAID-SIGN-1`) with a `debug_assert` direction test. 42
    mapper unit tests green. Committed `a0b64a7`; verified + finalized in the
    foundation cleanup pass (entity active-enums made `Copy`, `# Errors` doc
    sections + ownership-handoff `#[allow(needless_pass_by_value)]` on the
    fully-`Copy` read entry points, digit-separator + backtick fixes) so the CI
    pedantic gate (`-W clippy::pedantic -D warnings`) passes, not just the
    baseline clippy run.
  - `[x]` Migration layer (`budget-migration` crate, run #2): `Migrator`
    (`sea-orm-migration`) + the `m0001_genesis_schema` migration. Creates the 13
    Postgres `ENUM` types (`ENTITIES-12`, real pg enums, idempotent via
    `DO $$ ... duplicate_object` guards), all 10 SPEC §5 tables, an index on
    EVERY foreign-key column in the same migration (`SQL-DB-INDEX-1`), the
    repository lookup indexes (`SQL-DB-INDEX-2`: budget active-version,
    `categories.category_key`, `transactions(month_id, category_id)`,
    `transactions.date`, active-obligation-per-fund, plus the unique/dedup
    indexes which double as lookups), and the four §12 DB constraints. All
    `CREATE`s are `IF NOT EXISTS`, enum creates are exception-guarded, and `down`
    drops in reverse dependency order with `IF EXISTS`
    (`PROC-CI-MIGRATION-HYGIENE-1`). m0001 is the genesis create, so
    `ARCH-EXPAND-CONTRACT-1` has no prior shape to expand from; later breaking
    changes use the expand→migrate→contract split. Wired into
    `budget-infrastructure::run_pending_migrations` (idempotent `Migrator::up`)
    for startup. 10 DB-free structural tests (all 10 tables, all enum types,
    every FK index, the four §12 constraints, no-float-money, all-creates-
    idempotent, lineage/cutover columns, unique migration names) + 1
    `DATABASE_URL`-gated live up→up→down→up round-trip test
    (`ORCH-NEW-PATH-TESTS-1`). Money columns are `NUMERIC(18,2)`
    (`BUDGET-MONEY-1`), enums are real pg enum types (`ENTITIES-12`).

  **Constraint/migration layer verified (backend run #2):**
  `cargo fmt --all -- --check` clean; the CI pedantic clippy variant
  (`cargo clippy --workspace --all-targets --all-features -- -W clippy::pedantic
  -D warnings -D clippy::unwrap_used -D clippy::expect_used`) clean across all 6
  crates; `cargo test --workspace` green (90 passed = 38 domain + 42 mapper + 9
  migration-lib + 1 migration-integration; 1 deliberate `ignore` doctest);
  `unsafe_code = "forbid"` still active workspace-wide with zero `unsafe`. The
  live-Postgres migration round-trip test is gated on `DATABASE_URL` and
  no-ops in CI until Neon is provisioned (SPEC §12).

  **Foundation verified (backend run #1):** `cargo fmt --all -- --check` clean;
  `cargo clippy --workspace --all-targets --all-features -- -D warnings
  -D clippy::unwrap_used -D clippy::expect_used` clean AND the stricter CI
  pedantic variant (`-W clippy::pedantic`) clean; `cargo test --workspace` green
  (20 domain + 42 mapper = 62 tests, 1 deliberate `ignore` doctest);
  `unsafe_code = "forbid"` active workspace-wide with zero `unsafe` in sources.

- [x] **3. Repositories + UnitOfWork (DONE, backend run #3).**
  Six `Postgres*Repository` impls in `budget-infrastructure` — one per aggregate
  trait declared in `budget-domain` (`ARCH-REPO-PER-AGGREGATE-1`): User, Budget
  (+Category children), Month, Transaction, Fund (+RepaymentObligation
  children), PlaidItem (+Account children); plus PaycheckConfig. All reads return
  DOMAIN types via the `budget-mappers` crate (`ARCH-REPO-RETURNS-DOMAIN-1`); no
  `SeaORM` `Model` crosses a trait surface. Every method is `async`
  (`RUST-DOMAIN-7`/`REPO-3`). The `SeaOrmUow` + `SeaOrmUowProvider`
  unit-of-work primitive (`REPO-6`/`REPO-10`): concrete provider here, `UowProvider`
  trait stays in domain; the provider opens a tx, runs the boxed closure, then
  commits on `Ok` / rolls back on `Err`. Write paths route through a `with_conn`
  executor (`crate::conn::Executor` — a delegating `ConnectionTrait` over pool
  OR enlisted tx) so a repo write enlists in `Some(uow)` or uses the pool on
  `None` (`REPO-4`). Errors translate once in `crate::error::map_db_err`
  (`REPO-5`: unique/FK via `DbErr::sql_err()`; 40001/40P01 → `TransactionConflict`;
  `RecordNotFound` → `NotFound`; else `Database`). A generic `upsert` helper
  (`ON CONFLICT (pk) DO UPDATE` over all non-PK columns) backs every `save`.
  **Computed read surfaces** (`REPO-9`/`RUST-SEAORM-PROJECTION-TYPES-1`):
  `category_spent_for_month` + `month_net` were added to the `TransactionRepository`
  trait, returning new domain projection types `CategorySpent`/`MonthNet`
  (`budget_domain::projections`); both aggregate in a SINGLE SQL query
  (`SQL-DB-NPLUSONE-1`) via the raw-statement escape
  (`RUST-SEAORM-RAW-SQL-ESCAPE-1`, parsed into infra-local `FromQueryResult`
  rows) with `status IN ('settled','expected')` encoding
  `BUDGET-STATUS-DRIVES-INCLUSION-1` (the canonical polarity stays in
  `counts_in_budget`; the SQL comments cross-reference it). The
  `(month_id, category_id)` aggregation is served by the existing
  `ix_transactions_month_category` index — no new index needed.
  CRITICAL boundary held: `db.*`/`SeaORM` entity access lives ONLY in these
  repository modules (`ARCH-STRICT-LAYERING-1`).
  Rules: `ARCH-REPO-PER-AGGREGATE-1`, `ARCH-REPO-RETURNS-DOMAIN-1`,
  `RUST-DOMAIN-7`, `RUST-SEAORM-PROJECTION-TYPES-1`,
  `RUST-SEAORM-RAW-SQL-ESCAPE-1`, `SQL-DB-NPLUSONE-1`,
  `BUDGET-STATUS-DRIVES-INCLUSION-1`, `BUDGET-NO-DOUBLE-CHARGE-1`. **Done.**

  **Repository layer verified (backend run #3):** `cargo fmt --all -- --check`
  clean; the CI pedantic clippy variant
  (`cargo clippy --workspace --all-targets --all-features -- -W clippy::pedantic
  -D warnings -D clippy::unwrap_used -D clippy::expect_used`) clean across all 6
  crates (full `cargo clean` rebuild, exit 0, zero warnings/errors); `cargo test
  --workspace` green (**156 passed / 0 failed / 1 ignored** — 38 domain + 53
  infra-lib + 3 `DATABASE_URL`-gated live integration + 42 mappers + 9
  migration-lib + 1 genesis-integration; the 1 ignored is the deliberate `uow.rs`
  doctest). The infra-lib jump to 53 is run #3's `MockDatabase` unit suite
  (`repositories/mock_tests.rs`, 48 new + 5 pre-existing): exclusively
  `sea_orm::MockDatabase`, no live Postgres — covering error-translation
  (`REPO-5`), mapper round-trips, per-repo find/save, the
  `category_spent_for_month` + `month_net` raw-SQL aggregates
  (`REPO-9`/`BUDGET-STATUS-DRIVES-INCLUSION-1`: empty/single/multi/pending-excluded
  + zero/negative/positive/no-row), `find_rollover_for_month`/`find_rollover_bucket`
  (`BUDGET-ROLLOVER-INTEGRITY-1`), the full `TransactionStatus` inclusion-polarity
  matrix, and the `SeaOrmUow` downcast-rejection path (`REPO-6`). The 3 live
  integration tests in `tests/repositories_live.rs` no-op in CI until Neon is
  provisioned (SPEC §12) — they cover the save/find round-trip, the aggregate
  inclusion polarity, and UoW commit/rollback; they use `connect_fresh(&url)`
  rather than `.clone()` on `DatabaseConnection` (which drops `Clone` when the
  `mock` dev-feature is active). `unsafe_code = "forbid"` intact, zero unsafe.

  **RATIFIED 2026-06-07 — projection-type precedence (ROUTE-1); `RUST-SEAORM-PROJECTION-TYPES-1` amended in Camerata + project:**
  `RUST-SEAORM-PROJECTION-TYPES-1` literally says a projection "lives in the
  domain crate AND derives the `SeaORM` result-mapping trait." That conflicts
  with `RUST-DOMAIN-1` (domain crate forbids any `SeaORM` dep; `Cargo.toml`
  enforces it; the crate must stay WASM-clean). I reconciled by putting the
  projection STRUCTS in `budget-domain` (pure types, satisfying "lives in the
  domain crate" + `RUST-SEAORM-RAW-SQL-ESCAPE-1`'s "only typed domain types
  cross the trait boundary") while the `FromQueryResult` derive sits on a private
  infra-local row struct that maps into them. `RUST-DOMAIN-1` (structural,
  enforced) wins over the literal "derives the trait here" clause. This needs
  Zach's ratification (a one-line amendment to the projection rule) since it sets
  the precedent for every future projection.

- [x] **4. Month lifecycle + rolling Other (core differentiator, backend run #4).**
  `MonthLifecycleService` in `budget-app-services`
  (`crates/budget-app-services/src/month_lifecycle.rs`). `ensure_current_month`
  does LAZY init (`BUDGET-IDEMPOTENT-MONTH-INIT-1`): resolves the current
  `(year, month)` in `America/New_York` (D2, `ARCH-UTC-TIMESTAMPS-1`), finds the
  latest existing month, then creates EVERY missing month up to the current one
  in chronological order (multi-month gap = more loop iterations), resolving the
  budget version active on each month's 1st (`find_active_for_date`) and posting
  each month's rollover. Idempotent + concurrency-safe: month creation via
  `create_if_absent` (ON CONFLICT (user_id,year,month) DO NOTHING); rollover via
  `post_rollover_if_absent` — a `find_rollover_for_month` pre-check PLUS the
  partial-unique `transactions(month_id) WHERE is_rollover` backstop (a racing
  insert surfaces as `UniqueViolation`, treated as benign "already posted"). The
  ROLLOVER is a system transaction (`BUDGET-ROLLOVER-INTEGRITY-1`): one row
  `is_rollover=true`, category=the rollover bucket, dated the 1st, amount = the
  prior month's net leftover — never a mutated scalar. NET-LEFTOVER (D5) is the
  pure `net_leftover(actual_income, expected_income, expense_remaining)` =
  `(actual − expected) + Σ(expense remaining)`; `expense_remaining_sum` routes
  every row through `counts_in_budget` (`BUDGET-STATUS-DRIVES-INCLUSION-1`),
  excludes income rows (they net via the income term) and FUND-contribution rows
  (`BUDGET-FUND-EARMARK-1`/D6 — earmarked dollar counted once). Cross-aggregate
  create-month + post-rollover runs in ONE `UowProvider::run` closure
  (`SERVICE-TX-1`/`REPO-10`); service holds `Arc<dyn _>` deps (`SERVICE-DI-1`);
  no `db.*` here. INCOME SEAM (`crates/budget-app-services/src/income.rs`):
  `IncomeExpectation` trait (`expected_income(user, year, month) -> Money`) with
  `SemimonthlyFixedExpectation` (2 × amount, Zach's case) + `FixedExpectation`
  (injected/zero for the hourly degradation) — step 6 fills the full mode matrix
  behind it; lifecycle code never changes. 14 app-services tests (3 income + 11
  lifecycle: genesis/single-advance/multi-month-gap/year-boundary, triple-replay
  + multi-month-replay idempotency, fund-exclusion, pending-exclusion, D5
  cent-conservation, home-TZ-vs-UTC month boundary). **Done.**
  - **TRAIT-SURFACE FIX (ROUTE-1 — RATIFIED 2026-06-08; `RUST-DOMAIN-7` amended in Camerata + project to document the object-safe-core + extension-trait split):** `UowProvider`
    was documented as object-safe but was NOT — the generic default `run` method
    made `Arc<dyn UowProvider>` impossible to construct (the documented DI
    shape). Fixed minimally + behavior-preserving: the base trait now carries
    only the object-safe `run_boxed`; the ergonomic generic `run(..)` moved to a
    new `UowProviderExt` extension trait, blanket-impl'd for every
    `?Sized` provider (so both concrete `SeaOrmUowProvider` and `dyn
    UowProvider` get `.run()`). Callers add `use budget_domain::uow::UowProviderExt`
    (the infra live test was updated). The documented contract — `Arc<dyn
    UowProvider>` + `self.uow.run(..)` — is unchanged; this only makes it
    actually compile.
  Rules: `BUDGET-ROLLOVER-INTEGRITY-1`, `BUDGET-IDEMPOTENT-MONTH-INIT-1`,
  `BUDGET-FUND-EARMARK-1`, `ARCH-UTC-TIMESTAMPS-1` (D2), `SERVICE-TX-1`,
  `SERVICE-DI-1`, `BUDGET-STATUS-DRIVES-INCLUSION-1`.

  **Verified (backend run #4, final pass `f12c747`):** `cargo fmt --all -- --check`
  clean; the CI pedantic clippy variant (`cargo clippy --workspace --all-targets
  --all-features -- -W clippy::pedantic -D warnings -D clippy::unwrap_used
  -D clippy::expect_used`) clean across the workspace; `cargo test --workspace`
  green (**169 passed / 0 failed / 2 ignored** — 14 app-services-lib + 9
  app-services independent property/oracle (`tests/month_lifecycle_independent.rs`)
  + 38 domain + 53 infra-lib + 3 live + 42 mappers + 9 migration-lib + 1 genesis;
  the 2 ignored are the `uow.rs` doctest and the live-DB rollover-race harness,
  which belongs at the infra layer where real Postgres ON-CONFLICT/partial-unique
  isolation can be exercised). The independent suite re-derives the D5 net against
  its own `rust_decimal` oracle and its own in-memory fakes (never imports the
  build's fakes), so cent-conservation / idempotency / D5-net / D6-earmark are
  asserted non-tautologically; mutation checks confirmed coverage (a +$1 net
  mutation reddened 6 tests, a fund-exclusion removal reddened 3). No test exposed
  a correctness bug — `month_lifecycle.rs` carries zero net diff from the build.
  `unsafe_code = "forbid"` intact, zero unsafe. Added `chrono-tz` (workspace +
  app-services) for home-TZ month-membership; tokio (macros+rt) as an
  app-services dev-dependency to drive the async service tests (the crate itself
  stays runtime-free / WASM-clean).

  **Coverage boundary (not a bug):** `post_rollover_if_absent`'s "already posted
  -> benign no-op" branch is structurally unreachable under sequential replay (the
  catch-up anchor always advances strictly past any completed month), so the
  replay-idempotency tests verify loop-level idempotency; the guard's race branch
  is exercised only by the `#[ignore]`d live-DB harness against real Postgres
  isolation, not by in-memory fakes.

- [x] **5. Funds + large purchases + sinking funds. (CORRECTED to D6 Model A, `e70c702`.)**
  `FundService` (`crates/budget-app-services/src/fund.rs`): the one fund
  primitive in three kinds.

  **D6 RESOLUTION — Model A (`BUDGET-FUND-EARMARK-1`, SPEC §12 D6 + §4.9/§5,
  RESOLVED 2026-06-08).** The original step-5 build shipped Model B (total
  exclusion): fund CONTRIBUTIONS were dropped from the net via a fund-CATEGORY
  check in `counts_in_month_expense_remaining`, so an otherwise-zero month rolled
  over $0. That conflated contributions (which must COUNT) with draws (which must
  be excluded). Corrected to Model A: a fund contribution is a manual
  Other-bucket expense that COUNTS in the net, reducing the rolling Other by the
  contribution while the fund balance rises by the same amount — the earmarked
  dollar counted exactly once, no separate fund-balance subtraction from
  free-to-spend.

  **Mechanism:** a new explicit `is_fund_draw: bool` on `Transaction`
  distinguishes CONTRIBUTIONS (`false`, count) from DRAWS (`true`, excluded),
  replacing the old fund-category conflation. `counts_in_month_expense_remaining`
  (`budget-domain/src/predicates.rs`) now includes everything except income,
  `is_fund_draw` rows, and the obligation-keyed buffer-financed full-price row;
  the old `is_fund_contribution` helper was removed (and dropped from `lib.rs`
  re-exports). Contributions, sinking accruals, and buffer-repayment installments
  post with `is_fund_draw = false` (they COUNT, reducing rolling Other). Surplus
  draws (`draw_through_surplus`) and sinking payouts (`tag_sinking_payout`)
  post/mark `is_fund_draw = true` (excluded, not re-charged). Buffer-financed
  full-price stays `false` and is excluded via its obligation list (D7,
  unchanged). Persistence plumbing carries the new column: `budget-entities`,
  `budget-mappers` (read + write, round-trip test), and a new
  `is_fund_draw BOOLEAN NOT NULL DEFAULT FALSE` in `m0001_genesis_schema`.

  Large-purchase resolution (`LargePurchaseResolution`): `pay_in_full` (ordinary
  expense), `pay_through_surplus` (fund-draw, no repayment, excluded via
  `is_fund_draw`), `buffer_financed` (full-price tracking txn with ZERO budget
  impact + buffer draw + `repayment_obligation`; compulsory installments flow back
  into the buffer until `remaining=0 -> paid`, final installment clamped so cents
  reconcile exactly). Sinking funds: `accrue_sinking_fund` (`amount/period_months`
  into `Category::fund_balance`, carries over, posted as a contribution that
  COUNTS) + `tag_sinking_payout` (draws the reserve, re-assigns the bill to the
  sinking category, RESETS `next_due_date` one period forward — forward-looking
  accrual, marked `is_fund_draw`). `buffer_health` advisory flag only, never
  blocks. The buffer-financed full-price exclusion set comes from
  `FundRepository::list_buffer_financed_transaction_ids`. All cross-aggregate
  writes go through the `UowProvider` closure (`SERVICE-TX-1`). The step-4
  `expense_remaining_sum` and `month_lifecycle.rs` rollover row both route through
  the corrected predicate (rollover row carries `is_fund_draw = false`).
  Rules: `BUDGET-FUND-EARMARK-1` (D6 Model A), `BUDGET-NO-DOUBLE-CHARGE-1`,
  `SERVICE-TX-1`, `SERVICE-DI-1`, `BUDGET-MONEY-1` (D6/D7).

  VERIFIED (Model-A verification pass): `cargo fmt --all -- --check` clean; CI
  pedantic clippy (`-W clippy::pedantic -D warnings -D clippy::unwrap_used
  -D clippy::expect_used`) clean across the workspace; full `cargo test
  --workspace` green — **205 passed / 0 failed / 2 ignored** (28 app-services-lib
  + 17 `fund_independent` + 9 `month_lifecycle_independent` + 42 domain + 53
  infra-lib + 3 live + 43 mappers + 9 migration-lib + 1 genesis). `unsafe_code =
  "forbid"` enforced workspace-wide (`[workspace.lints.rust]`), zero unsafe in
  src. **Model A confirmed:** `earmark_contribution_counts_in_net_and_counted_once`
  asserts a $250 contribution -> rolling Other = −$250, fund balance = +$250
  (same arithmetic a $50 contribution gives: −$50 / +$50), with conservation
  `rollover + fund_balance == 0` (counted exactly once), checked against an
  INDEPENDENT `oracle_month_net` that re-derives the net without the build's
  exclusion logic. Unchanged invariants re-verified: buffer-financed full-price
  zero-month-impact (D7), installments restore-to-target, surplus draw not
  re-charged, sinking accrual + forward reset, and the step-4 rolling-Other suite
  (no regression). No new correctness bug surfaced by the verification pass.

- [x] **6. Income (BUILD semimonthly-fixed; STUB the rest). (DONE, backend run #6.)**
  The income engine fills the build-step-4 [`IncomeExpectation`] seam (`SPEC §4.8`,
  D5). The trait signature is **UNCHANGED** —
  `fn expected_income(&self, user, year, month) -> Money` (sync, infallible) — per
  the SPEC build-discipline + `ORCH-ONE-WAY-DOOR-1`; no widening was needed (see
  the "trait surface" note below).

  - **DRIVEN OFF THE PERSISTED CONFIG (point 1).** New
    `ConfigDrivenIncomeExpectation` (`crates/budget-app-services/src/income.rs`) is
    built from the user's `PaycheckConfig`, read via `PaycheckConfigRepository`
    (`db.*` stays in repositories, `ARCH-STRICT-LAYERING-1`). The async, fallible
    `load(repo, user)` (and the sync `from_config(&cfg)` core) resolves the config
    into a flat per-month figure ONCE at construction; the sync `expected_income`
    then returns that stored figure for any `(year, month)`. That is how a
    DB-driven, fallible, async resolution lives behind an unchanged sync infallible
    trait without widening it.
  - **BUILT FULLY: per_paycheck + semimonthly** (`2 × amount` every month, Zach's
    actual mode — no anchor/cadence arithmetic, no buffer, both `IncomeMode` values
    identical). **BUILT: the hourly/variable degradation** (`amount = None` for any
    cadence/mode -> `Money::ZERO` expected -> `actual - 0` flows straight into Other).
  - **STUBBED design-complete (no `todo!()`/`panic!`):** `smoothed` mode + smoothing
    buffer, and biweekly/weekly per-month paycheck counting. The full mode × cadence
    `match` is exhaustive; every unbuilt arm returns the new typed
    `ServiceError::UnsupportedIncome { mode, cadence, detail }` (added to a new
    app-services error enum `crates/budget-app-services/src/error.rs`,
    `RUST-DOMAIN-4`/`RUST-DOMAIN-6`) — loud-but-safe at the construction boundary, so
    an unbuilt config fails on `load()` rather than silently miscomputing. (A
    contradictory `hourly` + fixed-amount config is also surfaced loudly.)
  - **SURPLUS ROUTING (point 4).** `IncomeSurplusRouter` implements the
    `surplus_routing` default (`buffer | this_month | savings`) PLUS a
    per-transaction override (`effective_routing(default, override)`). `this_month`
    is a no-op (the D5 formula already raises Other by the surplus); `buffer`/`savings`
    REUSE the step-5 `FundService::contribute` plumbing (D6 Model A,
    `BUDGET-FUND-EARMARK-1`: the surplus becomes a counted Other-bucket expense that
    raises the target fund balance). Per-paycheck mode auto-raises Other by formula
    (no routing decision); the router is the smoothed-mode extension + the deliberate
    "add to this month" override path (`SPEC §4.8`).
  - **Tests:** 11 new app-services-lib unit tests (`income/tests.rs`): semimonthly =
    2 × amount across all 12 months; blank-amount degradation -> ZERO across the full
    cadence × mode grid; smoothed / biweekly / weekly / hourly-with-amount all
    surface `UnsupportedIncome` (no panic); config read via a fake repo; missing
    config -> domain error; mismatched-user -> ZERO (single-user defensive depth);
    surplus routing this_month-noop / buffer-contributes / missing-fund-errors /
    non-positive-errors. `SemimonthlyFixedExpectation` retained as a config-free
    constructor for the lifecycle tests; `FixedExpectation` retained for the
    hourly-zero injection + test driving.
  Rules: `SPIRIT-ROBUSTNESS-1` (design-complete, build-what-you-use),
  `ARCH-STRICT-LAYERING-1` (config read via repo), `BUDGET-FUND-EARMARK-1` (D6
  Model A surplus-into-fund), `BUDGET-NO-DOUBLE-CHARGE-1`, `SERVICE-DI-1`,
  `SERVICE-TX-1` (contribute is the step-5 UoW closure), `BUDGET-MONEY-1`,
  `RUST-DOMAIN-4`/`RUST-DOMAIN-6` (`ServiceError`). **Done.**

  **Verified (backend run #6):** `cargo fmt --all -- --check` clean; the CI pedantic
  clippy variant (`cargo clippy --workspace --all-targets --all-features -- -W
  clippy::pedantic -D warnings -D clippy::unwrap_used -D clippy::expect_used`) clean
  across the workspace; `cargo test --workspace` green (**219 passed / 0 failed / 2
  ignored** — app-services-lib 28 -> 39 with the 11 new income tests). `unsafe_code =
  "forbid"` intact, zero unsafe.

  **FINAL VERIFICATION PASS (backend run #6, post-test-suite — independent
  income tests landed in `8511826`):** re-ran all four gates from a clean tree.
  `cargo fmt --all -- --check` clean; CI pedantic clippy (`-W clippy::pedantic
  -D warnings -D clippy::unwrap_used -D clippy::expect_used`) clean workspace-wide;
  `cargo test --workspace` green — **234 passed / 0 failed / 2 ignored** (39
  app-services-lib + 17 `fund_independent` + 18 `income_independent` + 9
  `month_lifecycle_independent` + 42 domain + 53 infra-lib + 3 live + 43 mappers +
  9 migration-lib + 1 genesis; the 2 ignored are the `uow.rs` doctest + the live-DB
  rollover-race harness). The 18 `income_independent` tests re-derive the D5 income
  variance against an independent `rust_decimal` oracle (semimonthly = `2 × amount`
  feeds the net so income variance moves Other by EXACTLY the variance; blank
  amount -> actual flows straight to Other), confirm every stubbed mode/cadence arm
  surfaces the typed `UnsupportedIncome` (no panic/unimplemented on a reachable
  path), and confirm surplus routing reuses the step-5 fund invariants with the
  dollar counted once (`expense + balance = 0`). Step-4 + step-5 suites re-pass with
  no regression. `IncomeExpectation` trait signature UNCHANGED (sync, infallible);
  no ROUTE-1 triggered. `unsafe_code = "forbid"` intact, zero unsafe.

  **TRAIT SURFACE (point 5 — NOT widened, no ROUTE-1 needed).** The full mode matrix
  did NOT require widening `expected_income(user, year, month) -> Money`. The
  built paths (semimonthly = same figure every month; hourly = ZERO every month)
  are month-invariant, so a per-construction resolved figure satisfies the sync
  infallible trait. The fallibility the stubbed paths need is surfaced at the async
  `load()` boundary, not on the trait. Had a built path genuinely varied by month or
  needed `actual` passed in, that WOULD have been a trait widening and would have
  STOPPED per `structural_changes_route_not_auto` / `ORCH-ONE-WAY-DOOR-1`; it did
  not. See the report's "modeling ambiguity" note for one item surfaced rather than
  guessed.

- [x] **7. Auth + secrets. (DONE, backend run #7.)**
  The full app-level security layer (`BUDGET-AUTH-GATE-1`, `SPEC §9`/`§9.1`),
  single-user (ZERO multi-user code), no public signup. Built entirely inside the
  EXISTING crates — no new crate / module-topology boundary (the ROUTE-1 guard
  held: domain ports, infra impls + extractor, app-services orchestration, a
  `[[bin]]` under `budget-infrastructure`).

  - **DOMAIN (`budget-domain::auth`, WASM-clean ports):** the `WebauthnCredential`
    aggregate (`SPEC §5`, one user / many devices) + a `WebauthnCredentialId`
    newtype (`DOMAIN-2`); the single shared `AuthError` enum (`DOMAIN-6`,
    `InvalidCredentials` deliberately opaque = no user-enumeration oracle; no
    secret material in any variant); and the abstract port traits the service
    layer orchestrates against (`SERVICE-DI-1`): `PasswordHasher` (Argon2id),
    `TotpService` (RFC 6238), `SecretVault` (Key Vault read-by-reference,
    `BUDGET-PLAID-TOKEN-VAULT-1`), `WebauthnCredentialRepository` (`REPO-1`).
  - **ENTITY/MAPPER/MIGRATION:** `webauthn_credentials` entity (`ENTITIES-*`,
    belongs_to User + reverse has_many) + mapper + the `m0002_auth_schema`
    migration (FK index + `credential_id` UNIQUE in the same migration,
    `SQL-DB-INDEX-1/2`, `PROC-CI-MIGRATION-HYGIENE-1`; `ARCH-EXPAND-CONTRACT-1`
    appends rather than editing m0001). The session table is intentionally NOT
    defined here — the session store owns its own table (`SPEC §5`/`§9.1`).
  - **INFRA (`budget-infrastructure::auth`, concrete adapters):** `Argon2idHasher`
    (Argon2id PHC hash/verify); `Rfc6238TotpService` (enroll → secret +
    `otpauth://` URI, verify, `AUTH-1/2`); `WebauthnService` (webauthn-rs 0.5
    register + authenticate, passkey serialization, sign-counter clone
    detection); the `tower-sessions` Postgres session store + secure cookie policy
    (Secure + HttpOnly + SameSite=Strict, survives scale-to-zero); the
    **`AuthedUser` Axum extractor** = the enforce-by-construction gate (identity
    obtainable ONLY through `FromRequestParts`; no session → 401 + no data),
    proven by an in-crate minimal Axum harness (protected route taking
    `AuthedUser` 401s with no body unauthenticated, 200s with the authenticated
    `user_id` after a real login→cookie→protected round-trip); the `AzureKeyVault`
    `SecretVault` (managed identity, fail-safe typed errors, never logs the
    secret, `BUDGET-PLAID-TOKEN-VAULT-1`); and `PostgresWebauthnCredentialRepository`.
  - **APP-SERVICES (`budget-app-services::auth`):** `AuthService` orchestration
    (`SERVICE-DI-1`, `Arc<dyn _>` ports, stays WASM-clean): `verify_login`
    (password AND mandatory TOTP, opaque rejection + timing-equalized
    unknown-email), `enroll_totp`, `set_password`, passkey credential
    list/save/`complete_passkey_assertion` (counter-regression = clone rejection).
    Session create/validate/rotate/destroy are driven by the caller via the
    `tower_sessions::Session` handle (the mechanism is the infra/HTTP boundary);
    the service supplies the identity decision.
  - **SEED (`provision-user` `[[bin]]` under `budget-infrastructure`):** the
    out-of-band single-user provisioner (no public signup, `SPEC §9`) — Argon2
    hash + TOTP enroll + write the one `users` row, idempotent by email; prints
    the TOTP provisioning URI. A `[[bin]]` under the existing crate, NOT a new
    crate.

  **Verified (backend run #7):** `cargo fmt --all -- --check` clean; CI pedantic
  clippy (`-W clippy::pedantic -D warnings -D clippy::unwrap_used
  -D clippy::expect_used`) clean across the workspace; `cargo test --workspace`
  green — **269 passed / 0 failed / 2 ignored** (infra-lib 53 → 71 with 18 auth
  tests incl. the gate harness; app-services-lib 39 → 49 with the AuthService
  policy tests; +1 mapper for webauthn round-trip; +m0002 structural tests).
  `unsafe_code = "forbid"` intact, zero unsafe.

  Rules: `BUDGET-AUTH-GATE-1`, `AUTH-1/2`, `BUDGET-PLAID-TOKEN-VAULT-1`,
  `DOMAIN-2/3/4/6`, `REPO-1/2/4/5`, `ARCH-REPO-PER-AGGREGATE-1`,
  `ARCH-STRICT-LAYERING-1`, `SERVICE-DI-1`, `SPIRIT-ROBUSTNESS-1`,
  `SQL-DB-INDEX-1/2`, `PROC-CI-MIGRATION-HYGIENE-1`, `ARCH-EXPAND-CONTRACT-1`,
  `ORCH-NEW-PATH-TESTS-1`, `ROUTE-1` (no new crate; guard held).

  **ADVERSARIAL SECURITY-TEST PASS (`3c3201c`) + FINAL VERIFICATION (backend
  run #7, this pass):** 35 break-don't-confirm tests were added on top of run #7
  (Argon2 OWASP-param floor + plaintext-leak + corrupt-hash-never-authenticates;
  RFC 6238 Appendix-B SHA1 vectors + ±1-step skew window; a REAL virtual
  authenticator — `webauthn-authenticator-rs` `SoftPasskey` — driving full
  register+authenticate ceremonies with wrong-RP/cross-origin/tampered-sig/replay
  attacks in a NEW `tests/webauthn_ceremony.rs` integration file; cookie
  Secure+HttpOnly+SameSite=Strict on the real Set-Cookie; logout-destroys-session;
  forged/tampered/empty cookie → 401; deleted-user session fails closed; rejection
  is a bare `StatusCode` no-body carrier; user-scoping two-user seed). Re-ran all
  four gates from a touched-tree rebuild: `cargo fmt --all -- --check` clean; CI
  pedantic clippy (`-W clippy::pedantic -D warnings -D clippy::unwrap_used
  -D clippy::expect_used`) clean workspace-wide; `cargo test --workspace` green —
  **294 passed / 0 failed / 3 ignored** (infra-lib 88 + webauthn_ceremony 5 +
  app-services-lib 52 + 17 fund_independent + 18 income_independent + 9
  month_lifecycle_independent + 42 domain + 3 live + 43 mappers + 14 migration-lib
  + 1 genesis; the 3 ignored = `uow.rs` doctest, live-DB rollover-race harness,
  live Key-Vault round-trip). `unsafe_code = "forbid"` intact, zero unsafe; 6
  workspace members unchanged (ROUTE-1 held, no new crate/topology boundary).
  Steps 1–6 suites re-pass with no regression. Security properties confirmed:
  Argon2id password (OWASP params), mandatory TOTP 2FA, WebAuthn register/auth
  (phishing-resistant, clone-detecting), Postgres-backed Secure/HttpOnly/
  SameSite=Strict sessions surviving scale-to-zero, the `AuthedUser`
  enforce-by-construction gate (401 + no body without a valid session; identity
  ONLY via the extractor), per-user query scoping, Key-Vault reads fail-safe and
  never logging the secret value, single-user / no public signup.

  **DEFERRED TO THE FRONTEND PHASE (HTTP host + Dioxus UI, NOT this backend
  phase — per the ROUTE-1 structural guardrail):** (1) mounting the
  `SessionManagerLayer` + `AuthState` on the real Axum/Dioxus router and wiring
  the `AuthedUser` extractor onto every data-returning server-function (the gate
  is DEFINED + proven here against an in-crate harness, but no production route
  consumes it yet); (2) the login UI + the server-function login handler that
  CALLS `AuthService::verify_login` and writes the returned `user_id` into the
  `tower_sessions::Session` (this backend phase supplies the identity decision +
  the session handle contract, not the handler); (3) the passkey
  register/authenticate ceremony server-functions (the `WebauthnService` engine +
  `AuthService` persistence exist; the HTTP ceremony round-trip is frontend);
  (4) the TOTP-enrollment QR UI. Two non-blocking observations carried forward to
  that phase: `AuthedUser` has `pub` fields (an in-crate caller could
  struct-construct one, bypassing `FromRequestParts`; the by-construction property
  still holds because a `User` is only obtainable via a repo call — consider
  private fields + accessors when wiring the frontend); and the CI `test`/`clippy`
  jobs compile the OpenSSL-linked `webauthn-rs`/`webauthn-authenticator-rs` crates,
  which need `libssl-dev` + `pkg-config` (preinstalled on `ubuntu-latest`, and the
  Dockerfile builder stage already installs them, so prod + default CI build; an
  explicit apt step would harden against a future runner-image change).

- [x] **8. Plaid integration. (DONE, backend run #8.)**
  The full Plaid bank auto-pull layer (`SPEC §6`), built entirely inside the
  EXISTING crates — no new crate / module-topology boundary (the ROUTE-1 guard
  held): domain ports, infra HTTP client + sync engine, app-services
  orchestration.

  - **DOMAIN (`budget-domain::plaid_api`, WASM-clean ports + DTOs):** two
    abstract ports — [`PlaidApi`] (raw Plaid HTTP: create-link-token,
    exchange-public-token, cursor `/transactions/sync`) and [`PlaidSyncEngine`]
    (the higher-level cursor-sync/reconcile mechanics) — plus the raw Plaid DTOs
    (`PlaidTransaction`/`PlaidAccount`/`PlaidSyncPage`, Plaid-native sign, never
    interpreted here), `LinkTokenRequest` (+ `assert_no_money_movement` guard),
    `PlaidProduct` (Transactions/Accounts allowed; Transfer present ONLY so the
    guard can refuse it), `SyncSummary`, and the typed `PlaidError`
    (`DOMAIN-6`; never carries token material). The `SecretVault` port gained a
    `set_secret` method (write the token at exchange time); the only impl is the
    Azure client.
  - **MAPPER (`budget-mappers::transactions`, the single sign-flip site):**
    `plaid_dto_to_domain(dto, …) -> Transaction` flips the Plaid sign ONCE
    (`BUDGET-PLAID-SIGN-1`) via the shared `flip_plaid_amount` helper (the
    existing `plaid_model_to_domain` was refactored to reuse it, so the flip +
    direction guard live in exactly one place). Plaid rows land
    `category_id = NULL` (uncategorized) here.
  - **INFRA (`budget-infrastructure::plaid`, concrete adapters):**
    `HttpPlaidApi` (reqwest, rustls; Transactions(+Accounts) product only — the
    money-movement guard runs BEFORE any Plaid call; the `access_token` +
    `client secret` are never logged, `BUDGET-PLAID-TOKEN-VAULT-1`); and
    `SeaOrmPlaidSyncEngine` — the cursor loop (`/transactions/sync` until
    `has_more = false`), applying `added` (cutover-guarded, deduped by the UNIQUE
    `plaid_transaction_id`, status from Plaid `pending`/settled), `modified`
    (upsert in place, preserves the user's category, drives the pending->settled
    transition by removing the superseded pending row linked via
    `pending_transaction_id`), and `removed` (delete — predicate-based fixed-
    category settlement reverses automatically, `BUDGET-SETTLE-ON-MATCH-1`), then
    the rolling 30-day reconcile clamped to `max(today − 30d,
    tracking_start_date)` (`BUDGET-CUTOVER-1`). Every page's writes + the cursor
    advance commit in ONE unit-of-work transaction (`SERVICE-TX-1`). The
    `AzureKeyVault` `set_secret` writes the token. Idempotent: re-running never
    duplicates or double-applies.
  - **APP-SERVICES (`budget-app-services::plaid_sync::PlaidSyncService`):** the
    use-case entry (`SERVICE-DI-1`, `Arc<dyn _>` ports, WASM-clean, no `db.*`):
    `create_link_token` (Transactions-only), `exchange_and_link` (exchange →
    write token to Key Vault → persist the `PlaidItem` with ONLY the Key Vault
    reference; the raw token is dropped, never in the DB or a log,
    `BUDGET-PLAID-TOKEN-VAULT-1`), and `sync_user` (fetch the token from the
    vault by reference at call time, then delegate to the engine with the user's
    `tracking_start_date`, `BUDGET-CUTOVER-1`).

  **Verified (backend run #8):** `cargo fmt --all -- --check` clean; the CI
  pedantic clippy variant (`-W clippy::pedantic -D warnings -D
  clippy::unwrap_used -D clippy::expect_used`) clean across the workspace; `cargo
  test --workspace` green — **311 passed / 0 failed / 3 ignored** (4 new
  app-services service tests + 7 new infra sync-engine integration tests + 3
  infra http-client unit tests + 3 infra sync-engine unit tests + the existing
  suites, no regression). All Plaid logic runs against a MOCKED `PlaidApi` — NO
  live Plaid call in any test (the one live HTTP path is Zach's deploy-time
  step). `unsafe_code = "forbid"` intact, zero unsafe; 6 workspace members
  unchanged (no new crate). Confirmed: Transfer never enabled, token never in the
  DB.

  **MODELING AMBIGUITY SURFACED (ROUTE-1 — NOT self-approved):** `removed`
  reverses a FIXED-category settlement automatically (settlement is
  predicate-based: `fixed_category_spent` = `settled ? sum(rows) : placeholder`,
  so deleting the row reverts to the placeholder). But `BUDGET-SETTLE-ON-MATCH-1`
  also mentions "un-matching the EXPECTED expense" on `removed`, and there is NO
  persisted match-link between a real charge and an `expected` placeholder (the
  match action is a frontend §7 user action not built in this backend phase).
  Reversing an expected-expense match would require a new
  `matched_transaction_id` column (or equivalent) on `transactions` — a schema +
  semantics change. Per the structural guardrail I STOPPED rather than invent it;
  this needs Zach's decision before the §7 match UI / step 9.

  Rules: `BUDGET-PLAID-TOKEN-VAULT-1`, `BUDGET-PLAID-SIGN-1`, `BUDGET-CUTOVER-1`,
  `BUDGET-SETTLE-ON-MATCH-1`, `BUDGET-STATUS-DRIVES-INCLUSION-1`, `SERVICE-TX-1`,
  `SERVICE-DI-1`, `ARCH-STRICT-LAYERING-1`, `ARCH-UTC-TIMESTAMPS-1` (D2),
  `DOMAIN-6`, `ROUTE-1` (no new crate; guard held + one ambiguity surfaced).

  **FINAL VERIFICATION PASS (backend run #8, post-adversarial-tests):** re-ran all
  four gates from a clean tree (`0075828`). `cargo fmt --all -- --check` clean; the
  CI pedantic clippy variant (`cargo clippy --workspace --all-targets --all-features
  -- -W clippy::pedantic -D warnings -D clippy::unwrap_used -D clippy::expect_used`)
  clean across the workspace (exit 0, zero warnings); `cargo test --workspace` green
  — **319 passed / 0 failed / 4 ignored** (up from 311/3 with the 8 independent
  adversarial sync-engine tests in `tests/plaid_sync_engine_independent.rs` + 1
  `#[ignore]`d live-sandbox placeholder). `unsafe_code = "forbid"` enforced
  workspace-wide (`[workspace.lints.rust]`, line 80), zero `unsafe` in any source; 6
  workspace members unchanged (ROUTE-1 held, no new crate/topology boundary).
  Re-verified the §6 invariants against the source (not just test output): cursor
  `/transactions/sync` loop with added/modified/removed + the pending->settled
  transition removing the superseded pending row linked via `pending_transaction_id`
  (`sync_engine.rs:245-266`); dedup by the UNIQUE `plaid_transaction_id`
  (`apply_added` early-return) + idempotency (cursor advance + upsert); the rolling
  30-day reconcile clamped to `max(today − 30d, tracking_start_date)`
  (`reconcile_lower_bound`, the clamp IS the cutover guard); the `BUDGET-CUTOVER-1`
  pre-genesis exclusion on the added/modified/reconcile paths (`dto.date <
  tracking_start_date` → skip); sign normalization through the single
  `flip_plaid_amount` mapper site (`BUDGET-PLAID-SIGN-1`); Transfer never enabled
  (`assert_no_money_movement` refuses any money-movement product BEFORE any Plaid
  call, `http_client.rs:266`); and the `access_token` persisted ONLY as a Key Vault
  reference (`exchange_and_link` writes the token to the vault, `drop`s the
  plaintext, persists `PlaidItem` with `access_token_ref` only — never raw in DB or
  log). Steps 1–7 suites re-pass with no regression. No correctness bug surfaced;
  the one initially-red independent test was a test-mock setup artifact (empty
  cursor page consumed the reconcile re-pull), fixed in the test without weakening
  it. The ROUTE-1 expected-expense un-match ambiguity above is the only open
  decision; it does not block step 9. **Step 8 done.**

- [x] **9. Initial-load seeding. (DONE, backend run #9 — the LAST backend step.)**
  The genesis opening-snapshot layer (`SPEC §4.6`, `§4.3`, `§12` D8), built
  entirely inside the EXISTING crates — no new crate / module-topology boundary
  (the ROUTE-1 guard held): an `OnboardingService` in `budget-app-services` + a
  `seed-onboarding` `[[bin]]` under `budget-infrastructure` (mirroring the step-7
  `provision-user` bin).

  - **SERVICE (`budget-app-services::onboarding::OnboardingService`):** the
    use-case entry (`SERVICE-DI-1`, `Arc<dyn _>` ports, WASM-clean, no `db.*`).
    `seed(&OnboardingInput, now)` reads `tracking_start_date` from the persisted
    user (the single-source genesis boundary, `BUDGET-CUTOVER-1`), resolves the
    budget version active on the genesis date + its rollover ("Other") bucket
    (`SPEC §4.1`), resolves/creates the **genesis month** (the month containing
    day 1) WITHOUT posting a rollover (no backfill — the genesis month has no prior
    month; the starting-Other line IS its carryover), then in ONE
    `UowProvider::run` closure (`SERVICE-TX-1`) posts: (1) one settled manual
    EXPENSE per non-zero per-category opening charge, dated the genesis boundary,
    magnitude = month-to-date spend before tracking began (it COUNTS in budget math
    like real spend, `BUDGET-STATUS-DRIVES-INCLUSION-1`); a `$0` charge is SKIPPED
    so a `flexible_set` like utilities settles normally later; (2) the single
    starting rolling-Other opening line on the rollover bucket
    (`is_rollover = false` — onboarding opening line, NOT a system month-to-month
    rollover), signed balance verbatim; and (3) sets the starting buffer-fund
    balance DIRECTLY on the fund as a pre-genesis fact — NOT also posted as an
    Other-bucket contribution (those dollars were already expensed in the
    un-tracked pre-genesis world; posting them again would double-count against the
    starting Other, `BUDGET-FUND-EARMARK-1` / D6 Model A: each earmarked dollar
    counted exactly once; fund balances are not separately subtracted from
    free-to-spend). Reuses the step-4/step-5 primitives (the rollover bucket +
    month repos, the fund balance write); does not re-derive them.
  - **INPUTS are RUNTIME, never hardcoded** (the structural guardrail): the typed
    `OnboardingInput` (per-category `CategoryOpeningCharge` list + signed
    `starting_other_balance` + optional `BufferOpeningBalance`) carries Zach's
    actual figures, supplied at runtime by the bin from the reference xlsx. The
    service hardcodes no figures.
  - **CUTOVER AGREEMENT (`BUDGET-CUTOVER-1`):** onboarding writes NO transaction
    dated before `tracking_start_date` — the opening positions are dated ON the
    boundary (`== tracking_start_date`, valued as of end-of-day-0). Plaid sync +
    the rolling 30-day reconcile exclude everything dated STRICTLY before it
    (`< tracking_start_date`, the step-8 clamp). The two layers therefore meet
    exactly at the boundary and never double-count: the pre-genesis world is in the
    snapshot and nowhere else.
  - **RE-RUNNABLE / IDEMPOTENT (`SPEC §12` onboarding path):** every opening row is
    keyed by a DETERMINISTIC `TransactionId` (`opening_charge_id` /
    `opening_other_id` — a frozen-namespace UUIDv5 over (user, role, category)), so
    the existing `save` upsert (ON CONFLICT (pk) DO UPDATE) makes a re-run produce
    identical state (no duplicated charges, no double-counted balances), mirroring
    step-4 `create_if_absent` + the step-7 idempotent-by-key bin. The buffer
    balance is SET (not accumulated). Re-seeding revised figures after a test phase
    performs a clean coherent replace (test phase -> clean reset to real day 1).
  - **CLEAN MONTH-START (point 6):** because the opening Other line + opening
    charges are real settled rows in the genesis month, the FIRST step-4
    `MonthLifecycleService` rollover OUT of the genesis month computes a correct
    prior-month net over them (`BUDGET-ROLLOVER-INTEGRITY-1`) — verified by a test
    (genesis Other +$212, groceries −$300 -> August rollover = −$88).
  - **SEED (`seed-onboarding` `[[bin]]` under `budget-infrastructure`):** wires the
    concrete Postgres repos + `SeaOrmUowProvider` to the service; reads
    `tracking_start_date` from the provisioned user (run `provision-user` first),
    figures from a `SEED_CONFIG` JSON file (money parsed from decimal strings, no
    float touches a monetary value, `BUDGET-MONEY-1`). A fresh connection per
    component (DatabaseConnection is not Clone under the `mock` dev-feature); all
    writes commit on the provider's connection via the unit of work.
  Rules: `BUDGET-CUTOVER-1`, `BUDGET-ROLLOVER-INTEGRITY-1`, `BUDGET-FUND-EARMARK-1`
  (D6 Model A), `BUDGET-STATUS-DRIVES-INCLUSION-1`, `BUDGET-MONEY-1`, `SERVICE-TX-1`,
  `SERVICE-DI-1`, `ARCH-STRICT-LAYERING-1`, `ARCH-UTC-TIMESTAMPS-1` (D2),
  `ROUTE-1` (no new crate; guard held), `ORCH-NEW-PATH-TESTS-1`. **Done.**

  **Verified (backend run #9):** `cargo fmt --all -- --check` clean; the CI
  pedantic clippy variant (`cargo clippy --workspace --all-targets --all-features
  -- -W clippy::pedantic -D warnings -D clippy::unwrap_used -D clippy::expect_used`)
  clean across the workspace; `cargo test --workspace` green — **327 passed / 0
  failed / 4 ignored** (up from 319/4 with the 8 new onboarding service tests:
  per-category charges dated the boundary + $0 skipped, no row before the boundary,
  starting-Other line on the bucket, buffer SET without double-posting, re-run
  idempotency, revised-figure coherent replace, clean month-start first-rollover
  correctness, genesis-month-without-rollover). `unsafe_code = "forbid"` intact,
  zero unsafe; no new crate / topology boundary.

  **ROUTE-1 NOTE (dependency edge, NOT a new boundary):** the `seed-onboarding`
  bin needs `OnboardingService` (in `budget-app-services`) AND the concrete repos
  (in `budget-infrastructure`). To keep `budget-app-services` WASM-clean
  (domain-only, D1) the bin lives in `budget-infrastructure`, which now carries a
  `budget-app-services` dependency used ONLY by the binary (the infra LIBRARY does
  not use app-services). This adds one internal dependency EDGE
  (infra -> app-services), introduces no cycle (app-services depends solely on
  budget-domain), no new crate, and no public-trait/module-boundary change — it
  matches the `provision-user` bins-in-infra precedent. Surfaced for visibility per
  the structural guardrail, not self-approved as a topology change.

---

## Frontend phase (OUT OF SCOPE for this build)

- [ ] **10. chorale transactions UI.** The grouped/aggregated/in-cell-editing
  transactions table on chorale via the chorale `main` git dependency. Separate
  phase; built with manual QA (checker-poor half).

---

## STUB explicitly (design-complete, build-what-you-use)

- `smoothed` income mode + the income smoothing buffer (dormant for Zach; schema present).
- bi-weekly / weekly / hourly paycheck cadence resolution (only semimonthly built).
- Multi-user anything (schema is `user_id`-shaped; zero multi-user code, `SPEC.md` §9).
- The conservative in-month "Other" live view (`SPEC.md` §4.3/§10) unless Zach wants it.
