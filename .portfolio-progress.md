# AI Portfolio Insights — build progress (resumable checkpoint)

Phases 2–5 built against mocks, zero network. Phase 6 wires the real provider
edges + the UI (v1). V2 (Phase 7, DRIP & real-time tracking) is COMPLETE through
P7.4; P7.5 (delta refinement) is OUT of scope. One commit per logical unit; not
pushed.

Green gate per phase = `cargo build --all-targets` + `cargo test` +
`cargo clippy --all-targets` + `cargo fmt --check` all green (default features),
plus `cargo test -p budget-ui --features server` for the server-gated UI tests.
(The pre-existing chorale `[paths]` override warning is allowed.)

| phase | commit | default-feature workspace tests | notes |
|-------|--------|--------------------------------|-------|
| Phase 2 — manual sources + positions UI | 4fad3e3 | 496 | green |
| Phase 3 — market data + live portfolio_snapshot | 72c0990 | 507 | green |
| Phase 4 — advisor port + mock + fixtures | 352d515 | 519 | green |
| Phase 5 — reconcile + use-case + terminal states + audit | 86e59b3 | 553 | green |
| Phase 6.1 — review_runs mapper + PostgresReviewRunRepository | (per-unit) | — | green |
| Phase 6.2 — real GeminiAdvisor HTTP adapter | (per-unit) | — | green |
| Phase 6.3 — market-data chain Finnhub→Stooq→manual→None | (per-unit) | — | green |
| Phase 6.4 — run_review + AI_MODE + review_run_to_dto + model list | (per-unit) | — | green |
| Phase 6.5 — UI review screen (dropdown + cards + disclaimer) | (per-unit) | 586 | green |

Phase 6 COMPLETE (586 default-feature workspace tests; 46 budget-ui `--features
server`). v1 done.

## V2 — DRIP & Real-Time Position Tracking (`docs/DRIP_REALTIME_DESIGN.md`)

Built forward on the same branch. Each slice green-gated (`build --all-targets` +
`test` + `clippy --all-targets` + `fmt --check`). The pre-existing chorale
`[paths]` warning is allowed.

| phase | default-feature workspace tests | notes |
|-------|--------------------------------|-------|
| P7.1 — data model + ports + domain | 601 | green |
| P7.2 — dividend sources + cache + DRIP repo | 623 | green |
| P7.3 — catch-up engine (mock-only) | 637 | green |
| P7.4 — wire-in + UI (per-account upsert, toggle, badge) | 654 | green |

**P7.1 (data model + ports + domain) — DONE.**
- Migration `m0008_drip_realtime`: `positions.drip_enabled` (BOOLEAN NOT NULL
  DEFAULT false) + `positions.baseline_as_of` (TIMESTAMPTZ NOT NULL DEFAULT
  now()); NEW `dividend_events` (ticker-keyed cache, unique `(ticker, pay_date)`,
  no user FK) + `drip_applications` (append-only DRIP chain, FK→positions Cascade,
  unique `(position_id, pay_date)` idempotency guard, FK + `(user_id, applied_at)`
  history indexes). All idempotent/expand-only. 6 DB-free structural tests.
- Entities: `positions` gains the two columns; new `dividend_events` +
  `drip_applications` entities.
- Mappers: `positions` carries the two fields; new `dividend_events` (identity-free
  projection + `to_active_model`) + `drip_applications` (full aggregate) mappers
  with round-trip + corruption-rejection tests.
- Domain: `DividendEvent`, `DividendSourceKind`, `ShareProvenance`,
  `DripApplication`; `DividendSource` port + `DividendSourceError`;
  `PricedPosition.share_provenance`; `Position.{drip_enabled, baseline_as_of}`.
  New IDs `DividendEventId` / `DripApplicationId`. New repo ports
  `DividendEventCache` + `DripApplicationRepository`. Serde round-trip tests.
- `price_position` now defaults provenance to `Uploaded`; added
  `price_position_with_provenance` (the P7.4 DRIP seam).

**P7.2 (dividend sources) — DONE.** `crates/budget-infrastructure/src/dividend_sources/`:
- `MockDividendSource` (fixture, filters by `pay_date > since`) + `ManualDividendSource`
  (static tier, forces Manual provenance) FIRST.
- `TiingoDividendSource` (key from vault `tiingo-api-key`; daily-prices `divCash`
  parser) + `YahooDividendSource` (keyless v8 `events=div` parser) — each a PURE
  parser unit-tested against a captured/sample payload, never a live call. Date
  semantics: both free endpoints carry no pay-date, so `date` is used as ex==pay
  (best-effort `ORCH-TRAINING-CUTOFF-1`, self-correcting on re-upload §2.1).
- `ChainDividendSource` (Tiingo → Yahoo → manual): first non-empty tier wins;
  empty/error falls through (logged WARN), same resilience shape as the market chain.
- `dividend_events` cache repo (`PostgresDividendEventCache`): `find_since` +
  idempotent `upsert_many` on `(ticker, pay_date)`.
- `drip_applications` repo (`PostgresDripApplicationRepository`): `apply_if_absent`
  (ON CONFLICT (position_id, pay_date) DO NOTHING → returns inserted bool) +
  `list_for_position` (chronological).

**P7.3 (catch-up engine, mock-only) — DONE.**
`crates/budget-app-services/src/portfolio_drip/`:
- `config.rs`: `DripConfig` (pinned `DRIP_BUFFER=0.10` exact decimal, `DRIP_SHARE_DP=3`).
- `compute_accretion` (PURE §3 math): chronological compounding; DRIP-on
  `shares_added = floor(raw_new × (1 - buffer), share_dp)` via `RoundingStrategy::ToZero`
  (floor; accreted shares positive); buffer scoped to ACCRETION only (baseline never
  haircut); DRIP-off `cash_added = amount × shares_held` exact, no buffer.
- `PayDatePriceSource` port (the existing market chain resolves only the CURRENT
  quote; a missing pay-date price HOLDS the event, §3).
- `DripCatchUpService.catch_up_position`: cache-through dividend fetch; per-event
  price resolution (held if none); §3 accretion; `apply_if_absent` per row under the
  `(position_id, pay_date)` guard; DRIP-off → credit account `CashBalance`
  (BUDGET-CASH-1) only on a NEWLY-applied row; current shares RECOMPUTED from the
  persisted chain (BUDGET-ROLLOVER-INTEGRITY-1), provenance labeled.
- 14 mock tests: idempotency/re-entrancy (apply twice → no change, no double cash),
  chronological compounding, DRIP-on vs off, buffer + FLOOR (1.285 not 1.286),
  suppression of `pay_date <= baseline_as_of`, held-event (no price), incremental
  later-dividend apply.

**P7.4 (wire-in + UI) — DONE. V2 COMPLETE.**
- **DRIP wire-in:** `assemble_snapshot_with_drip` (app-services) runs the P7.3
  idempotent catch-up engine per position during snapshot assembly (concurrent,
  `ARCH-PARALLEL-INDEPENDENT-1`), replaces `shares` with the estimated current
  count (`BUDGET-ROLLOVER-INTEGRITY-1`), and carries each position's
  `ShareProvenance` label onto the `PricedPosition` (`BUDGET-AI-1`). The
  `portfolio_snapshot` server fn + `GeneratePortfolioReview` (via `with_drip`)
  both assemble through it, so the AI review reconciles against estimated values
  WITH the label. `PortfolioState` gains an `Arc<DripCatchUpService>` built in
  `from_connections`: dividend source selected by `AI_MODE` (`MockDividendSource`
  under mock; `Tiingo→Yahoo→manual` `ChainDividendSource` on the real path),
  `PostgresDividendEventCache` + `PostgresDripApplicationRepository`, and a
  `MarketQuotePayDatePriceSource` adapter (in `server_state`, the server-only
  wiring layer — infra LIBRARY must not depend on app-services) approximating the
  pay-date price with the current quote (flagged best-effort,
  `ORCH-TRAINING-CUTOFF-1`). Uses the real `StooqMarketData` type.
- **Provenance badge:** `PricedPositionDto` gains `shares_estimated` +
  `estimated_badge` (a HUMAN string rendered at the DTO boundary; raw discriminant
  never crosses, `RUST-DIOXUS-10`). The priced-positions chorale table gains a
  "Share count" column ("confirmed" vs the estimate badge).
- **Per-account upsert (correctness-critical, §2.7/§6):** `upsert_account` runs
  the whole reconcile in ONE transaction (`RUST-SEAORM-INTRA-AGGREGATE-TX-1`). A
  pure `plan_account_upsert` decides deletes/updates/inserts: the removal sweep is
  FILTERED to the uploaded account (existing loaded `WHERE account_label =`), so
  uploading Brokerage never touches Roth/IRA; survivors re-baselined with
  `drip_enabled` PRESERVED; new holdings inserted DRIP-off. Server fn
  `upload_account_positions` (`AccountUploadDto`, validates at the boundary).
- **DRIP toggle:** `set_drip_enabled` repo method is a targeted single-column
  update (only `drip_enabled` + `updated_at`), so the toggle survives uploads
  (per-position config, never a re-baseline). Server fn `set_drip_enabled`.
- **UI:** holdings `PositionsByAccount` (grouped by account, keyed rows,
  `RUST-DIOXUS-8`) with an inline DRIP checkbox per row; a lean `UploadAccountForm`
  (account label/type + `TICKER, SHARES[, COST_BASIS]` paste).
- **Tests (new paths, `ORCH-NEW-PATH-TESTS-1`):** snapshot-with-drip prices
  estimated shares + labels provenance + idempotent reopen; badge renderer; the
  per-account upsert planner (drip preserved + re-baseline; sold-off removed +
  new DRIP-off); a `DATABASE_URL`-gated LIVE test proving uploading Brokerage
  leaves Roth's positions + their `drip_enabled` untouched; `group_by_account`,
  `parse_upload_text`, `uploaded_dto_to_domain` parsers.

P7.5 (delta refinement) OUT of scope.

## Verification boundary (honest)

The live Gemini / Finnhub / Stooq / **Tiingo / Yahoo dividend** network paths
CANNOT be exercised here (no API key, no network). Verified via: `AI_MODE=mock`
end-to-end; `MockDividendSource` driving the DRIP catch-up engine through
snapshot assembly; the captured-response fixture for `gemini.rs`'s parse path;
Stooq/Finnhub/chain + Tiingo/Yahoo dividend adapters unit-tested against
captured/sample payloads; the per-account upsert planner + a `DATABASE_URL`-gated
live cross-account isolation test; the full green gate (build/test/clippy/fmt +
wasm32 check). The TRUE live dividend smoke test (real Tiingo/Yahoo key + network,
confirming the dividend chain returns ex/pay-date + amount) is Zach's to run, as
is the Gemini/Finnhub live smoke test.

## Open items for Zach (best-effort / config / one routed decision)

- **Live wire shapes (`ORCH-TRAINING-CUTOFF-1`):** confirm Gemini `usageMetadata`
  placement (per-candidate vs top-level — `WireCandidate` currently reads it
  per-candidate; if top-level, move the field to `GeminiResponse`) and the
  JSON-in-text vs inline-structured-object response. Confirm Finnhub `/quote` +
  Stooq CSV column shapes at smoke time.
- **Config knobs:** `GEMINI_MODEL_IDS` (allow-list, seeded default
  `gemini-2.5-pro,gemini-2.5-flash`), `AI_MODE=mock` (mock opt-in), `KEY_VAULT_URL`
  (real path), vault secrets `gemini-api-key` + `finnhub-api-key`,
  `BUDGET_USER_EMAIL` (see below).
- **`BUDGET_USER_EMAIL` (single-user binding):** the cash-balance adapter is
  user-bound (SPEC §9) and the user is provisioned out-of-band, so `main.rs`
  resolves the single user at startup from this env var. If unset/unfound the
  portfolio Extension is not mounted (routes 500 "state unavailable",
  Plaid-optional posture). This startup-binding is the one wiring decision the
  design left open; confirm the approach.
- **Context enrichment (decision #2 nice-to-have):** fundamentals + ≤3 headlines
  per position is seam-flagged `// Phase 7:` in `gemini.rs` (needs unverifiable
  per-position network calls + prompt-budget capping), not built.
